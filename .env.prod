# --- Core secrets ---
SECRET_KEY=replace-with-a-long-random-secret

# --- Hosts ---
# Your production hostnames (no scheme)
ALLOWED_HOSTS=example.com,www.example.com

# CSRF trusted origins MUST include scheme (https://) and hostname
CSRF_TRUSTED_ORIGINS=https://example.com,https://www.example.com

# --- Database (Postgres via psycopg) ---
# Format: postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+psycopg://myuser:mypassword@db-host:5432/nursery

# --- HTTPS/Proxy ---
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=15552000  # (optional) ~180 days after you're confident
